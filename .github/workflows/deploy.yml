name: Deploy Next.js to cPanel

on:
  push:
    branches:
      - production

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.4'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build
        env:
          NEXT_PUBLIC_ROOT_DOMAIN: vuongtran.io.vn

      - name: Prepare Deployment Files
        run: |
          mkdir -p deploy
          # 1. Copy core standalone (chứa server.js và node_modules tối giản)
          cp -r .next/standalone/. deploy/
          # 2. Copy static và public để chạy giao diện
          cp -r .next/static deploy/.next/static
          cp -r public deploy/public
          # 3. Tạo file khởi chạy deploy.js
          echo "process.env.NODE_ENV = 'production';
          process.env.PORT = process.env.PORT || 3000;
          require('./server.js');" > deploy/deploy.js
          
          # 4. NÉN: Vào thư mục deploy để nén, tránh lồng thư mục khi giải nén
          cd deploy && zip -r ../build.zip . && cd ..

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: ${{ secrets.FTP_PORT || 21 }}
          protocol: ftp
          local-dir: ./
          # VÌ PATH FTP CỦA BẠN ĐÃ LÀ /public_html/vuongtran NÊN ĐỂ ./
          server-dir: ./
          exclude: |
            **/*
            !build.zip

      - name: Trigger Unzip
        # File unzip.php nằm ở gốc public_html/vuongtran nên gọi trực tiếp qua domain
        run: curl https://vuongtran.io.vn/vuongtran/unzip.php